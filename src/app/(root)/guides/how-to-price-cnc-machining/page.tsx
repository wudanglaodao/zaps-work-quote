import type { Metadata } from "next";
import Link from "next/link";
import { GuideArticle } from "@/components/guide-article";
import { buildMetadata } from "@/lib/seo";
const title = "How to Price CNC Machining: Material, Setup, Machine Time, and Batch Cost"; const description = "Build a 3-axis CNC milling estimate from material, programming, setup, cycle time, inspection, tooling, batch size, and margin.";
export const metadata: Metadata = buildMetadata({ locale: "en", path: "guides/how-to-price-cnc-machining", title, description });
export default function Page() { return <GuideArticle title={title} description={description} published="2026-07-14" updated="2026-07-14" readingTime="7 min read" slug="how-to-price-cnc-machining" toolHref="/tools/cnc-machining-cost-calculator" toolLabel="Open calculator" toc={[{ id: "material", label: "Material and scrap" }, { id: "time", label: "Time and batch" }, { id: "margin", label: "Quote the order" }]} faq={[{ question: "Does this calculator generate CAM times?", answer: "No. Enter your CAM and shop assumptions. The calculator makes their cost effect transparent." }, { question: "Why does quantity change unit price?", answer: "Programming and setup are fixed order costs, while cycle and inspection repeat per part." }]}><p className="guide-lead">A CNC quote is an order-cost model first and a per-part price second. Material, programming, setup, machine time, inspection, tooling, and finishing do not all scale at the same rate with quantity.</p><div className="guide-callout"><strong>The short formula</strong><code>Order price = max(total direct cost ÷ (1 − target margin), minimum order fee)</code></div><h2 id="material">1. Price the stock, not only the finished part</h2><p>Start with the purchased blank volume and material rate, then add an explicit scrap allowance for saw loss, workholding, rejected material, and usable remnant assumptions. Use the actual material and supplier conditions for the order.</p><h2 id="time">2. Separate fixed time from repeating time</h2><p>Programming and setup are order-level time. Cycle time and inspection repeat for each part. Machine rate should recover machine ownership, maintenance, and shop capacity; labor rate should reflect programming, setup, inspection, and handling time as you perform it.</p><h2 id="margin">3. Quote the batch transparently</h2><p>Add tooling, finishing, packaging, and subcontract work before applying margin. Show both order total and unit price, but do not let the per-part figure hide setup cost. The <Link href="/tools/cnc-machining-cost-calculator">CNC machining cost calculator</Link> is intentionally a manual 3-axis milling estimator, not a replacement for process planning or CAM verification.</p></GuideArticle>; }
