import type { Metadata } from "next"; import { Cpu } from "lucide-react"; import { notFound, permanentRedirect } from "next/navigation"; import { NewToolPage } from "@/components/new-tool-page"; import { isLocale } from "@/lib/i18n/config"; import { getNewToolCopy } from "@/lib/i18n/new-tools"; import { buildMetadata } from "@/lib/seo";
const path = "tools/cnc-machining-cost-calculator"; export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return isLocale(locale) ? buildMetadata({ locale, path, title: getNewToolCopy(locale, "cnc").title, description: getNewToolCopy(locale, "cnc").description }) : {}; } export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); if (locale === "en") permanentRedirect("/tools/cnc-machining-cost-calculator"); return <NewToolPage kind="cnc" locale={locale} path={path} guidePath="guides/how-to-price-cnc-machining" Icon={Cpu} />; }
