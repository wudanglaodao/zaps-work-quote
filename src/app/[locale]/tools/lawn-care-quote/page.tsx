import type { Metadata } from "next"; import { Sprout } from "lucide-react"; import { notFound, permanentRedirect } from "next/navigation"; import { NewToolPage } from "@/components/new-tool-page"; import { isLocale } from "@/lib/i18n/config"; import { getNewToolCopy } from "@/lib/i18n/new-tools"; import { buildMetadata } from "@/lib/seo";
const path = "tools/lawn-care-quote"; export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return isLocale(locale) ? buildMetadata({ locale, path, title: getNewToolCopy(locale, "lawn-care").title, description: getNewToolCopy(locale, "lawn-care").description }) : {}; } export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); if (locale === "en") permanentRedirect("/tools/lawn-care-quote"); return <NewToolPage kind="lawn-care" locale={locale} path={path} guidePath="guides/how-to-price-lawn-care-services" Icon={Sprout} />; }
